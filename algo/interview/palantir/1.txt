Given an array, find longest consecutive arithmetic sequence



Given an array, find longest arithmetic sequence (no need to be consecutive). 
Given a list of string, return the list of prefix that can uniquely define each string.
Given a string, return the number of characters you need to put at the end of the string in order for the string to be a palindrome.
然后有道debug题目，程序是用来implement a smart way of lexicographical comparator，问题是错误输出和slow execution time，然后需要找出其中的错误。
Given a list of weights (each one associated with a label), generate a random label based on the weight. 然后再优化提高速度。



多米诺游戏；
给一个 2 * n的矩阵，例如
1   6   3   4   -2   -7   9$ w3 N- @& k) v# I) C9 i* z
3   4   6   1   -3   0    2
然后给你k块多米诺板，每个板可以遮盖连续的两个数字，可以上下相邻两个，可以左右相邻两个。
不允许重叠覆盖，然后不允许斜着对角线覆盖。' D3 C: k3 j' `$ x3 d( k) r6 `" ?
允许使用少于k块板。$ ~* U7 c+ G, z  m
: D9 t+ }; R: l+ o

求可以遮盖的最大数字和是多少？   



第二轮问的是lru cache，还有一道题是说给定一个数组，这个数组有一个特性是，每一个元素距离其排序后的位置，最多差k个距离，然后叫我将这个数组排序，要求时间复杂度好于nlogn.


数组那个，可以把长度为n的数组，partition成 k 个 长度为 n/k 的数组，根据给出的property, 可发现这k个小数组都是sorted。然后维护一个size为k的 min heap, merge k sorted array 即可. 复杂度应该为n*logk
