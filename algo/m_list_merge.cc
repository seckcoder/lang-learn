
#define K 100


void merge_rec(int arrs[][K], int m, int *l, int *idx) {
  int min_idx = min(arrs, idx);

}

// merge m arrs into l
void merge(int arrs[][K], int m, int *l) {
  return merge_rec(arrs, m, int *l);
}
